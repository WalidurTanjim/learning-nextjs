const LoadingSingleMeal = () => {
    return (
        <section className={`loading-page w-full h-screen flex items-center justify-center bg-[#fff]`}>
            <h1 className={`text-2xl font-medium text-slate-700`}>Loading single meal...</h1>
        </section>
    );
};

export default LoadingSingleMeal;